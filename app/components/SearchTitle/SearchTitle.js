import React from 'react';
import {DisplayText} from '@shopify/polaris';

class SearchTitle extends React.Component {
  render() {
    return(
      <DisplayText size="extraLarge">Search for a book</DisplayText>
    );
  }
}

export default SearchTitle;
